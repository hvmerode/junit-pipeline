parameters:
- name: param_1
  type: string
  default: param_1_value
- name: param_2
  type: string
  default: param_2_value
steps:
- script: |
    echo 'This is step 1 of file template-steps.yml with parameter ${{ parameters.param_1 }}'
  displayName: Step D
- script: |
    echo 'This is step 2 of file template-steps.yml with parameter ${{ parameters.param_2 }}'
  displayName: Step E