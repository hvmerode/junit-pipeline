parameters:
- name: param_1
  type: string
  default: xxx
- name: param_2
  type: string
  default: param_2_value
steps:
- script: |
    echo 'This is parameter ${{ parameters.param_1 }}'
  displayName: template-steps.yml script
- script: |
    echo 'This is step 2 of file template-steps.yml'
  displayName: template-steps.yml script
